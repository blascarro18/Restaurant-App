{
  "name": "warehouse",
  "version": "1.0.0",
  "main": "dist/src/main.js",
  "scripts": {
    "start": "npm run prisma:generate && node dist/src/main.js",
    "start:dev": "npm run prisma:generate && npm run prisma:migrate && npm run prisma:seed:dev && ts-node-dev --respawn --transpile-only --poll src/main.ts",
    "prisma:generate": "npx prisma generate",
    "prisma:migrate": "npx prisma migrate deploy",
    "prisma:seed:dev": "npx ts-node prisma/seed.ts",
    "prisma:seed": "node dist/prisma/seed.js",
    "build": "tsc",
    "test": "jest"
  },
  "prisma": {
    "seed": "npm run prisma:seed"
  },
  "keywords": [
    "warehouse",
    "microservices",
    "warehouse management"
  ],
  "author": "Breyner Lascarro A.",
  "license": "ISC",
  "description": "Microservicio de gestión de bodega para el ecosistema de microservicios.",
  "devDependencies": {
    "@types/amqplib": "^0.10.7",
    "@types/axios": "^0.9.36",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.15.2",
    "jest": "^29.7.0",
    "prisma": "^6.6.0",
    "ts-jest": "^29.3.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "@prisma/extension-accelerate": "^1.3.0",
    "amqplib": "^0.10.7",
    "axios": "^1.9.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "dotenv": "^16.5.0",
    "reflect-metadata": "^0.2.2",
    "ts-node": "^10.9.2"
  }
}
